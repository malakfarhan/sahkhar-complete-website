<IfModule mod_rewrite.c>
  RewriteEngine On

  # Let PHP API requests through
  RewriteRule ^api/ - [L]

  # SPA fallback for frontend routes
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
